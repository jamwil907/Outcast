name: PHP CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.0'   # or whichever version you use
      - name: Install dependencies
        run: |
          # (if you had dependencies via composer, e.g.)
          # composer install
          # For now maybe nothing
          echo "No dependencies"
      - name: Run tests
        run: |
          php test_weather.php
